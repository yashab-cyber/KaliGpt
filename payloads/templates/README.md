# Payload Templates

This directory contains template files for various exploit payloads.

## Directory Structure

- `shells/` - Reverse shell templates
- `webshells/` - Web shell templates
- `exploits/` - Common exploit templates
- `privesc/` - Privilege escalation scripts
- `wordlists/` - Custom wordlist generators
